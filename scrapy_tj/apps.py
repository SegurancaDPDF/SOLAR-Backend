from django.apps import AppConfig


# fornece a configuração necessária para a aplicação 'scrapy_tj'
class MniwsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'scrapy_tj'
